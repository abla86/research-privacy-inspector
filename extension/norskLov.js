export const clean=(v="")=>String(v).replace(/\s+/g," ").trim();
export function detectLegalKind({shortTitle="",officialTitle="",url=""}={}){const s=`${shortTitle} ${officialTitle} ${url}`.toLowerCase();if(/forskrift/.test(s))return"REGULATION";if(/lovdata\.no\/lov\//.test(url)||/^lov/i.test(clean(shortTitle)))return"LAW";return"UNKNOWN";}
export function parseNorwegianLaw(input={}){const shortTitle=clean(input.shortTitle||input.title),officialTitle=clean(input.officialTitle),dateCode=clean(input.dateCode),url=clean(input.url),kind=detectLegalKind({shortTitle,officialTitle,url});const missing=[];if(!shortTitle)missing.push("shortTitle");if(!officialTitle)missing.push("officialTitle");if(!dateCode)missing.push("dateCode");if(!url)missing.push("url");return{kind,shortTitle,officialTitle,dateCode,url,missing,status:missing.length?"INCOMPLETE":"COMPLETE",verified:false};}
export function formatLegalInText({shortTitle,year,section}={}){const s=clean(shortTitle),y=String(year??"").trim();if(!s||!/^\d{4}$/.test(y))return"";return section?`${s}, ${section} (${y})`:`${s} (${y})`;}
export function buildNorwegianLegalReference(input={}){const p=parseNorwegianLaw(input),year=String(input.year??"").trim(),website=clean(input.websiteName||"Lovdata"),missing=[...p.missing];if(!/^\d{4}$/.test(year))missing.push("year");if(missing.length)return{reference:"",status:"INCOMPLETE",missing:[...new Set(missing)],verified:false};return{reference:`${p.shortTitle}. (${year}). ${p.officialTitle} (${p.dateCode}). ${website}. ${p.url}`,status:"COMPLETE",verified:false};}
